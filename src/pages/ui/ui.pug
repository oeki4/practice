extends ../../layout/layout

block variables
  -
    const pageName = 'ui'
    const pageTitle = 'ui'

block modal
  //- +empty-modal
  +response-modal(id = 'response-modal')
  +dealer-modal(id = 'dealer-modal')
  +telephone-modal(id = 'telephone-modal')
  +reset-password-modal(id = 'reset-password-modal')
  +change-password-modal(id = 'change-password-modal')
  +success-modal(id = 'success-modal')
  +test-modal


block content
  main
    .inner
      h1.title-64 H1 класс title-64
      h2.title-36 H2 класс title-36
      br
      br
      p.text-18  text-18
      p.text-22  text-22
      br
      br
      +select(['Мужской','Женский'])(placeholder='Пол')
      br
      br
      +input(placeholder='place')
      br
      br
      +button("Кнопка")
      br
      br
      +button("модалка-1")(data-modal-open='response-modal')
      +button("Dealer modal")(data-modal-open='dealer-modal')
      +button("Telephone modal")(data-modal-open='telephone-modal')
      +button("Reset Password")(data-modal-open='reset-password-modal')
      +button("Change password")(data-modal-open='change-password-modal')
      +button("Success")(data-modal-open='success-modal')
      br
      br
      +button("модалка-2")(data-modal-open='test-modal')
      br
      br
      +checkbox("текст")
      br
      br
      +textarea(placeholder='Имя', name='name')
      br
      br
      +picture("test.jpg",true)
      +pictureRandom()
      +pictureRandom()
